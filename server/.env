# ============================================================
# Astrologer CRM – Server Configuration
# ============================================================

# MongoDB Connection URI
# Option 1 – Local MongoDB (if MongoDB is installed locally):
# MONGO_URI=mongodb://localhost:27017/astrologer-crm

# Option 2 – MongoDB Atlas (free cloud):
# Sign up at https://cloud.mongodb.com → Create free cluster
# Replace with your actual Atlas connection string:
MONGO_URI=mongodb+srv://navridhi1004_db_user:arstro@cluster0.eiyubby.mongodb.net/astrologer-crm?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret Key (change this to any long random string)
JWT_SECRET=astrologer_crm_super_secret_jwt_key_2024

# Server Port
PORT=5000
